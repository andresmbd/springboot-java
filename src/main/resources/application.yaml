spring:
  application:
    name: catalogo-productos
  datasource:
    url: jdbc:h2:mem:catalogobd
    username : sa
    password :
    driver-class-name : org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true